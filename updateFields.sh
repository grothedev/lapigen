#!/bin/bash

#TODO
#this script should be run if model and migration files have already been generated (and edited) and models.txt has then been changed.
#it will update the fields in the model and migration files
